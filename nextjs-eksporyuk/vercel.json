{
  "version": 2,
  "buildCommand": "rm -rf .next && npm run build",
  "installCommand": "npm install --force && rm -rf .next node_modules/.cache",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "FORCE_REBUILD": "v5.2.3-css-layer-fix",
    "NEXT_DISABLE_SWC_CACHE": "1"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/r/:username",
      "destination": "/api/r/:username"
    },
    {
      "source": "/go/:username/:path*",
      "destination": "/api/r/:username"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
